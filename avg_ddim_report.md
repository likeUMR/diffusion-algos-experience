# Avg-DDIM 小报告

## 算法思路

`Avg-DDIM` 是在 DDIM 的训练目标上做的改进，采样过程仍然沿用原来的 DDIM 反向采样，不改变推理公式。

原始 DDIM 训练时，对每个真实数据点 `x0` 随机采样时刻 `t` 和噪声 `epsilon`，得到加噪点 `x_t`，然后让模型预测这个噪声方向。

`Avg-DDIM` 的改动是：在得到 `x_t` 后，不只使用原始 `x0` 对应的噪声作为监督，而是额外采样若干个候选原始数据点，组成总数为 `k` 的候选集合。对每个候选 `x0_i`，计算它在当前时刻生成同一个 `x_t` 所需要的噪声方向，以及对应的前向扩散概率。最后按概率加权这些噪声方向，得到一个平均监督目标。

直观上，它不是强迫模型只学习“这个点来自唯一某个 `x0` 的噪声方向”，而是让模型看到局部数据流形附近多个可能来源的加权平均方向，从而降低训练监督的随机性，让去噪场更平滑。

## 当前最佳设置

本轮最好的版本使用：

- 候选总数：`k=30`
- 额外采样点数：`29`
- 补充点采样方式：开启高斯拒绝采样
- 高斯标准差：`0.3`
- 其他训练和采样参数：完全复用 DDIM 在 HPO 中对应 NFE 的最佳参数

高斯拒绝采样的作用是让补充候选点更偏向原始样本附近，而不是从全局数据集中均匀抽取。这样加权平均更像局部流形平均，避免远处点对监督方向产生干扰。

## 效果对比

指标为最终双向 Chamfer Distance，越低越好。

| NFE | DDIM | Avg-DDIM k=30 均匀采样 | Avg-DDIM k=30 高斯采样 std=0.3 |
| ---: | ---: | ---: | ---: |
| 100 | `0.007316` | `0.005429` | `0.004435` |
| 20 | `0.009754` | `0.008224` | `0.007737` |
| 5 | `0.022123` | `0.022651` | `0.018706` |
| 1 | `0.315133` | `0.312884` | `0.288564` |

相对原 DDIM，高斯采样版 Avg-DDIM 的提升约为：

| NFE | 提升幅度 |
| ---: | ---: |
| 100 | `39.4%` |
| 20 | `20.7%` |
| 5 | `15.4%` |
| 1 | `8.4%` |

所有新实验的 Coverage 都达到 `100%`。

## 结论

`Avg-DDIM` 在当前二维螺旋数据任务上明显优于原始 DDIM。均匀采样版已经能在高 NFE 下提升效果，而加入高斯拒绝采样后，四个 NFE 桶全部超过 DDIM，尤其是 `NFE=5` 从原本略差变成明显更好。

这说明该方法的关键收益来自“局部候选点的概率加权平均”：它能让训练目标更贴近数据流形局部结构，同时保持 DDIM 原本的采样框架不变。代价是训练阶段会更慢，尤其开启拒绝采样后计算量增加，但在效果上收益明显。
