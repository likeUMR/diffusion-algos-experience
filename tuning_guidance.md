# 二维生成模型在“训练算力对齐下”的极限调参指导手册
*(Tuning Guidance under Training Computational Budget Constraints)*

## 1. 核心理念与痛点分析：为什么要卡“训练算力”且不卡“推理采样”？

在学术界和工业界的模型评估中，人们常常陷入一个绝对化的公平误区：强行让所有算法在训练时运行相同的 Epochs，同时在推理时使用相同的步数（NFE）。

然而，**这种死板的参数对齐，是对各种算法特性的极大抹杀！**

### 💡 为什么“卡训练总算力，不卡采样步数”更加合理？
1. **训练成本是核心瓶颈**：在真实场景中，模型训练需要消耗数天、数周甚至数月的 GPU 算力（包含大量的反向传播求导运算），是研发成本的大头；而采样的推理成本（通常仅需要前向传播）极低。因此，**控制训练阶段的总算力消耗，让推理采样可以自由开大步数**，才最符合真实的工程实践。
2. **训练迭代与模型表征的互补性**：根据计算复杂度理论，训练阶段的总算力可以通过两个维度互补平衡：
   $$\text{训练总算力 (Training Total FLOPs)} = \text{训练 Epochs} \times \text{每Epoch的Batches数} \times \text{单Batch训练等效 NFE} \times \text{前向 FLOPs}$$
   * **小模型，多 Epochs**：可以用更轻量、计算快的网络，训练极多次数（例如 800 Epochs），让参数有更长的时间进行精细梯度更新。
   * **大模型，少 Epochs**：可以用极具表征能力的巨型网络，训练较少次数（例如 50 Epochs），凭借特大模型强大的空间流形插值能力，实现“降维打击”。
3. **推理采样彻底解放**：DDPM 依赖长马尔可夫链退火，Flow Matching 和 DDIM 依赖常微分方程（ODE）求解。现在你可以自由把采样步数（NFE）开到 **50步、100步甚至1000步**，只要它们在训练阶段的“算力额度”是公平对齐的。

---

## 2. 训练算力评估工具与“算力分配天花板”

在框架中，单次神经网络前向传播的计算量 $F(H, B, T)$ 是根据隐藏层维度 $H$、残差块数 $B$、时间嵌入维度 $T$ 来精确估算的（对应函数 `get_forward_flops`）。

### 📐 训练等效算力换算原理
由于反向传播（Backward pass）比前向传播（Forward pass）大概多花费一倍计算量，通常：
* **标准算法的单 Batch 训练算力**（适用于 DDPM, DDIM, VDM, V-Learning, Flow Matching, MeanFlow）：
  * 仅包含 1 次前向和 1 次求导反向 $\Rightarrow$ 等效于 **$3$ 次前向计算量**：
    $$\text{单 Batch 训练 FLOPs} = 3 \times F(H, B, T)$$
* **自一致性算法的单 Batch 训练算力**（适用于 Consistency Models）：
  * 包含 1 次在线网络前向及反向（折算为 3 次前向），以及 1 次无梯度的目标网络前向（折算为 1 次前向） $\Rightarrow$ 等效于 **$4$ 次前向计算量**：
    $$\text{单 Batch 训练 FLOPs} = 4 \times F(H, B, T)$$

---

### 🎯 经典训练总算力对齐黄金配表 (基准：约 40.8G FLOPs)
如果你想与全局默认的 **`flow_matching` (epochs=200, H=256, B=4)** 的训练算力对齐：
* 数据集总数 $N = 20,000$，Batch Size = $512 \Rightarrow$ 每 Epoch 包含 $39$ 个 Batches。
* 默认尺寸前向 $F(256, 4, 256) \approx 1.74$M FLOPs。
* **目标基准总算力**：$200 \text{ Epochs} \times 39 \text{ Batches} \times 3 \times 1.743872\text{M} \approx \mathbf{40.81\text{G FLOPs}}$。

以下是在该总训练预算下，允许 $\pm 10\%$ 安全波动的各算法配平方案：

| 算法类型 | 配置风格 | 训练 Epochs | 专属模型尺寸 [H, B] | 单次前向 FLOPs | 等效单Batch NFE | 训练总 FLOPs | 算力偏差比 (Deviation) |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **Flow Matching / DDPM** | 标准基准 | **200** | `256 x 4` (time=256) | 1.74M | 3 | **40.81G** | `0.0%` (基准) |
| **极轻量高频更新型** | 极速收敛型 | **780** | `128 x 3` (time=128) | 0.44M | 3 | **40.32G** | `-1.2%` (安全) |
| **中量级均衡型** | 深度代表 | **90** | `384 x 4` (time=384) | 3.86M | 3 | **40.57G** | `-0.6%` (安全) |
| **超重量级大模型** | 极限拟合力 | **51** | `512 x 4` (time=512) | 6.78M | 3 | **39.73G** | `-2.6%` (安全) |
| **Consistency Models (CM)**| 标准模型 | **150** | `256 x 4` (time=256) | 1.74M | 4 | **40.81G** | `0.0%` (安全) |
| **Consistency Models (CM)**| 大模型折算 | **38** | `512 x 4` (time=512) | 6.78M | 4 | **40.14G** | `-1.6%` (安全) |

---

## 3. 🎯 巅峰效果的“最后一公里”：“算力无关”超参精调指南

在确定了训练算力分配（模型尺寸与 Epoch 轮数）后，模型效果能不能发挥到 100% 的绝对极限，完全取决于**“算力无关”的优化器超参数**：**学习率 (Learning Rate, LR)** 与 **权重衰减 (Weight Decay, WD)**。

### 🧪 3.1 学习率 (LR) 的物理本质与缩放定律
学习率决定了模型在参数流形中迈出的每一步梯度大小。

1. **模型尺寸缩放法则 (LR Scaling Law)**：
   * **大模型方案 (如 $H=512$, $Epochs=51$)**：
     * **特点**：参数量变大，其承受学习率剧烈波动的耐受性（Gradient Sensitivity）会变弱，在大步长下极其容易出现局部梯度过载（导致 loss 曲线震荡，甚至报 NaN）。
     * **调参动作**：**必须适度降低 LR**。推荐精调在 `8.0e-4 ~ 1.0e-3` 之间，让大模型稳健、匀速地雕琢复杂的 2D 流形。
   * **小模型方案 (如 $H=128$, $Epochs=780$)**：
     * **特点**：网络容量小，但专属 Epoch 轮数极其充裕。
     * **调参动作**：**可以适当调大 LR**。推荐精调在 `1.8e-3 ~ 2.5e-3` 之间。在极多的迭代次数下，较大的步长可以帮助轻量网络快速穿过局部极小值（Local Minima），彻底逼近分布的骨架。

2. **算法本身的 LR 偏好度**：
   * **Flow Matching / V-Learning**：采用的是直线概率流或速度直接 L2 回归，损失函数平滑，对高 LR 的兼容性极其恐怖。可放心开到 `1.5e-3 ~ 2.0e-3`。
   * **VDM (变分扩散模型)**：由于在连续时间上积分优化 VLB（变分下界），其梯度带有极强高频噪声，对大 LR 极其敏感。**强烈建议将 LR 控制在 `6.0e-4 ~ 9.0e-4` 之间**，否则极易发生概率坍塌。
   * **Consistency Models (CM)**：由于目标网络与在线网络在训练时处于瞬时的 EMA 自一致对齐状态，大学习率会拉断学生网络与教师网络之间的自一致局域共性。推荐 LR 为标准的 `1.0e-3 ~ 1.5e-3`。

---

### 🎨 3.2 权重衰减 (Weight Decay) 与流形宽度控制
权重衰减的物理本质是对全连接层矩阵的 $L_2$ 正则化，限制其高频振荡，控制模型生成的 2D 点云在“流形法线方向”上的溢出程度。

1. **一维窄流形的特殊性质**：
   * 在我们训练的 2D 复杂海螺线数据中，数据在数学上是一条一维的流形，也就是说它的**流形厚度理论上应该等于 0**（所有点应当完美贴在螺线上）。
   * 如果网络发生了轻微的**过拟合**，或者在插值时引入了过多不必要的**高频不规则特征**，模型生成的点云就会呈现为一条“胖带子”、“粗麻绳”或布满刺眼的“毛刺噪声”。

2. **利用 Weight Decay 锁死流形宽度**：
   * **毛刺多、点云太胖（流形距离 Dist 偏高）**：
     * **物理分析**：网络拟合了过多的高频空间细节，导致去噪路径在局部产生了不规则的小型涡流。
     * **调参动作**：**逐步调大权重衰减 (Weight Decay)**！从默认的 `1e-5` 提升到 `5e-5`、`1e-4` 甚至 `5e-4`。大 WD 能狠狠压制神经元权重的绝对值，把不必要的高阶高频弯曲特征“修剪掉”，让去噪场在空间中极其平滑，生成的点云自然会死死收缩至完美的一维螺线上，流形均距指标瞬间暴降！
   * **断点、空洞多（流形覆盖率 Coverage 偏低）**：
     * **物理分析**：权重衰减过大，限制了模型的表达灵活性，导致网络无法表达海螺线局部超高曲率（例如中心极度弯曲的区域）的几何特征，致使其在弯折最强的地方直接“松手”，形成大片无样本的“空洞”。
     * **调参动作**：**果断调小权重衰减 (Weight Decay)**！将 WD 降至 `1e-6` 甚至 `0`（完全松绑）。给大隐层模型的局部表征能力彻底松手，让它能够精细拟合出螺旋中心最密集的几何极值点，将流形覆盖率直接逼上 100.00% 的极限。

---

## 4. 七大生成算法：极限微调超参黄金对照表

下面是所有算法在**训练总算力 40.8G FLOPs 完全对齐**的背景下，想要压榨出绝对极限（Manifold Dist 最小，Coverage 接近 100%）的优化器与物理超参黄金配置指南：

### (1) DDPM (Denoising Diffusion Probabilistic Models)
* **算力配平**：`epochs: 200` | 尺寸: `256 x 4`
* **极限优化超参**：
  * **`lr`**: `1.5e-3` (训练极度平稳，可略微拉大 LR 加速中低频收敛)
  * **`weight_decay`**: `1e-5` (默认) 至 `3e-5` 之间。对于窄流形，调大 WD 可以显著收紧点云带宽度。
* **物理采样(不卡算力)**：
  * **`num_steps` (采样)**: **`100` 步**。
  * **`beta_end`**: `0.16`（必须通过拉高终点 beta 彻底清除噪声偏置）。
  * **`variance_type`**: 锁死为 `"fixed_small"`。

---

### (2) DDIM (Denoising Diffusion Implicit Models)
* **算力配平**：
  * *方案 A (标准)*：`epochs: 200` | 尺寸: `256 x 4`
  * *方案 B (大模型)*：`epochs: 51` | 尺寸: `512 x 4` (大网络在 50 epoch 即可完美提炼几何拓扑)
* **极限优化超参**：
  * **`lr` (方案 A)**: `1.5e-3` | **`lr` (方案 B)**: `9e-4` (大模型必须调小学习率，防止前期 Loss 崩溃)
  * **`weight_decay`**: `5e-5` (适度拉大，帮助 ODE 轨迹在空间上维持最顺滑的骨架)
* **物理采样(不卡算力)**：
  * **`sample_steps` (采样)**: **`100` 步**（高 NFE 配合 ODE 常微分轨道带来极致的几何保真度）。
  * **`eta`**: 设为 `0.0` (完全隐式确定性去噪，消除任何随机噪声注入带来的点云分散)。

---

### (3) VDM (Variational Diffusion Models)
* **算力配平**：`epochs: 350` | 尺寸: `192 x 3` (必须牺牲模型宽度，给连续变分损失争取足够的 Epoch 迭代收敛周期)
* **极限优化超参**：
  * **`lr`**: 极其严格控制在 `7.5e-4 ~ 9e-4`。
  * **`weight_decay`**: `1e-6` 极小（因为变分 VLB 损失自带强约束，过大 WD 会使模型失去表达复杂拓扑的能力）。
* **物理参数**：
  * `gamma_min`: `-4.8` | `gamma_max`: `4.8`。

---

### (4) V-Learning (Velocity-based / v-prediction)
* **算力配平**：`epochs: 90` | 尺寸: `384 x 4` (中量级完美均衡方案)
* **极限优化超参**：
  * **`lr`**: `1.2e-3`。
  * **`weight_decay`**: `2e-5` 左右。
* **物理采样(不卡算力)**：
  * **`num_steps` (采样)**: **`100` 步**。
  * **`beta_end`**: `0.16`（使速度场终点干净去偏）。

---

### (5) Flow Matching (流匹配)
* **算力配平**：`epochs: 51` | 尺寸: `512 x 4` (超重量级大模型方案。FM 在此配置下将展现出彻底的统治力)
* **极限优化超参**：
  * **`lr`**: `1.0e-3` (大模型微调 LR) 或者是标准的 `1.5e-3 ~ 2.0e-3`（标准 200 epochs 配置下）。
  * **`weight_decay`**: `1e-4` (流匹配对正则化有极好的抗性，调大 WD 会把生成的螺线裁剪到如剃刀般纤细和精准)。
* **物理采样(不卡算力)**：
  * **`num_steps` (采样)**: **`80` 步** (连续 ODE 积分完全不怕步数限制，尽显高算力推理优势)。

---

### (6) Consistency Models (一致性模型 - CM)
* **算力配平**：`epochs: 150` | 尺寸: `256 x 4` (自一致等效训练算力对齐)
* **极限优化超参**：
  * **`lr`**: `1.2e-3`。
  * **`weight_decay`**: `1e-5`。
* **物理参数与采样(不卡算力)**：
  * **`sample_steps` (采样)**: **`3` 步** 或 **`5` 步** (使用 3~5 步的少步数快速迭代积分修正，可以彻底校正单步自一致误差，获得远超 1 步的巅峰效果)。
  * **`ema_decay`**: 在 150 epochs 的短训练下，**必须调小至 `0.92 ~ 0.95`**。
  * **`sigma_data`**: 锁定为 `0.5`。

---

### (7) MeanFlow (均值概率流)
* **算力配平**：`epochs: 100` | 尺寸: `352 x 4`
* **极限优化超参**：
  * **`lr`**: `1.1e-3`。
  * **`weight_decay`**: `1e-5`。
* **物理参数与采样(不卡算力)**：
  * **`p` (损失范数控制)**: **坚决设为 `0.5` (Powered L2 Loss)**。
  * **`c`**: `0.001`。
  * **`num_steps` (采样)**: **`5` 步** 或者是 **`8` 步**。

---

## 5. 终极对决调参：五步工程实操指南

### 📅 第一步：定义当前对决的“训练总预算”
打开 `config.yaml` 文件的 `train` 区域。
* 确保全局的 `epochs`, `batch_size`, `n_samples` 设置完备。系统默认会以 `train.budget_benchmark_algorithm`（通常是 `flow_matching`）在全局模型配置下的训练总消耗（epochs * batches * 3 * F）作为目标预算。

### 📐 第二步：利用等效算力公式配平参数
对各个算法分别配置各自独立的专属 `epochs`、`hidden_dim`、`num_blocks` 等。
请反复使用此原则：
* **需要大量迭代收敛的算法 (VDM, CM)** $\Rightarrow$ 维持较轻量的模型（如 `256x4`），保持较高的专属 `epochs`（如 150 或 200）。
* **拟合稳健、偏爱大容量网络的算法 (Flow Matching, DDIM, V-Learning)** $\Rightarrow$ 缩减专属 `epochs`（如 51 轮），直接将模型宽度顶满至 `hidden_dim: 512`（单次前向 FLOPs 暴增至 6.78M）。

### 🚀 第三步：精调学习率与权重衰减
打开 `config.yaml` 中的各个具体算法参数卡：
* 如果对该算法使用了**大尺寸模型**，请**立即将学习率调低**到 `8e-4 ~ 1e-3`。
* 如果对该算法使用了**轻量网络方案**，请**将学习率调高**到 `1.8e-3 ~ 2.2e-3`。
* 观察最终生成的图像：如果边缘点云弥散、太胖，**大幅调大 `weight_decay` 到 `1e-4`**；如果局部产生断头、空洞，**大幅调小 `weight_decay` 到 `1e-6`** 释放拟合张力。

### ⚙️ 第四步：彻底解放推理采样步数
在各具体算法的配置项中，自由将采样步数推向完美。
* DDPM / DDIM / V-Learning: 设置 `num_steps: 100`。
* Flow Matching: 设置 `num_steps: 50`。
* Consistency Models: 设置 `sample_steps: 3` (快速纠偏迭代)。
* MeanFlow: 设置 `num_steps: 5` (高阶积分纠偏)。

### 📊 第五步：运行训练偏差校验
在终端运行评测：
```bash
python main.py --mode benchmark
```
在训练启动前，系统会自动打印出 **「算力守门员 训练算力校验看板」**。
* 仔细检查每一行算法的“训练 FLOPs (G)”和“预算偏差”。
* 如果某个算法由于模型太大或 epochs 太高触发了 `[WARN]`（偏差超过了 $\pm 10\%$），请通过成比例调小它的 `epochs` 或者微调 `hidden_dim` 把它调回安全合规的 PASS 区。
